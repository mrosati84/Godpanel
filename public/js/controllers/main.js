angular.module('godpanel', [])
.controller('MainCtrl', function ($scope, $http) {
    // $scope.range = function (num) {
    //     return new Array(num);
    // };

    // $http.get('/api/v1/projects')
    //     .success(function (data, status, headers, config) {
    //         $scope.projects = data;
    //     });

    // $http.get('/api/v1/users')
    //     .success(function (data, status, headers, config) {
    //         $scope.users = data;
    //     });
});
